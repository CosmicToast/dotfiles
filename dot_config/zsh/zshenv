# vim: ft=zsh
: ${ZDOTDIR:=~/.config/zsh}

xsource() {
	local i
	for i; do
		[ -r "$i" ] && source "$i"
	done
}
xzsource() {
	xsource "${@/#/$ZDOTDIR/}"
}

xzsource zshenv.pre

xzsource zshenv.{local,post}
